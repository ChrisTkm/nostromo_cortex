services:
  mongo:
    image: mongo:7
    container_name: cortex-local-mongo
    restart: unless-stopped
    profiles:
      - local-mongo
    ports:
      - "127.0.0.1:27018:27017"
    volumes:
      - ./data/mongo:/data/db
